const op = require('os')

console.log(op.arch()) //This will show the architecture of the system

console.log(op.platform()) //This will show the platform we are running this

console.log(op.networkInterfaces()) //This will show the network information

console.log(op.cpus())